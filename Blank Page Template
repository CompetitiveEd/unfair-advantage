<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page Title | Site Name</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        /* --- DESIGN SYSTEM --- */
        :root {
            /* Palette */
            --primary: #3b82f6;      /* Blue */
            --amber: #f59e0b;        /* Amber */
            --red: #ef4444;          /* Red */
            --green: #10b981;        /* Green */
            
            /* Light Mode Default */
            --text-heading: #1e293b;
            --text-body: #334155;
            --text-muted: #94a3b8;
            --bg-body: #f8fafc;
            --bg-card: #ffffff;
            --border-dynamic: #e2e8f0;
            --nav-border: #cbd5e1;
            
            --font-main: 'Inter', sans-serif;
            --max-width: 800px;
        }

        /* Dark Mode Support */
        @media (prefers-color-scheme: dark) {
            :root {
                --text-heading: #f1f5f9;
                --text-body: #cbd5e1;
                --text-muted: #64748b;
                --bg-body: #0f172a;
                --bg-card: #1e293b;
                --border-dynamic: #334155;
                --nav-border: #334155;
            }
        }

        /* --- GLOBAL STYLES --- */
        * { box-sizing: border-box; }

        body {
            font-family: var(--font-main);
            background-color: var(--bg-body);
            color: var(--text-body);
            line-height: 1.6;
            margin: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        a { color: var(--primary); text-decoration: none; transition: opacity 0.2s; }
        a:hover { opacity: 0.8; }

        /* --- LAYOUT CONTAINERS --- */
        .container {
            width: 100%;
            max-width: var(--max-width);
            margin: 0 auto;
            padding: 0 1.5rem;
        }

        /* --- HEADER / NAV --- */
        header {
            background-color: var(--bg-body); /* Or bg-card for distinct look */
            border-bottom: 1px solid var(--border-dynamic);
            padding: 1.5rem 0;
            margin-bottom: 2rem;
        }

        .nav-flex {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .site-logo {
            font-weight: 700;
            font-size: 1.1rem;
            color: var(--text-heading);
            letter-spacing: -0.02em;
        }

        nav a {
            margin-left: 1.5rem;
            color: var(--text-body);
            font-size: 0.9rem;
            font-weight: 500;
        }
        
        nav a:hover { color: var(--primary); }

        /* --- MAIN CONTENT TYPOGRAPHY --- */
        main {
            flex: 1; /* Pushes footer down */
            padding-bottom: 4rem;
        }

        h1 {
            color: var(--text-heading);
            font-size: 2.25rem;
            font-weight: 700;
            letter-spacing: -0.03em;
            margin-top: 1rem;
            margin-bottom: 0.5rem;
            line-height: 1.2;
        }

        h2 {
            color: var(--text-heading);
            font-size: 1.5rem;
            font-weight: 600;
            margin-top: 2.5rem;
            margin-bottom: 1rem;
            letter-spacing: -0.02em;
        }

        h3 {
            color: var(--text-heading);
            font-size: 1.125rem;
            font-weight: 600;
            margin-top: 2rem;
            margin-bottom: 0.75rem;
        }

        p { margin-bottom: 1.25rem; }

        .lead {
            font-size: 1.125rem;
            color: var(--text-body);
            margin-bottom: 2rem;
            opacity: 0.9;
        }

        hr {
            border: 0;
            border-top: 1px solid var(--border-dynamic);
            margin: 2rem 0;
        }

        /* --- COMPONENTS --- */
        
        /* Standard Card */
        .card {
            background-color: var(--bg-card);
            border: 1px solid var(--border-dynamic);
            border-radius: 8px;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            box-shadow: 0 1px 2px rgba(0,0,0,0.05);
        }

        /* Button */
        .btn {
            display: inline-block;
            background-color: var(--primary);
            color: white;
            padding: 0.6rem 1.2rem;
            border-radius: 6px;
            font-weight: 500;
            font-size: 0.9rem;
        }
        .btn:hover { opacity: 0.9; color: white; }

        /* --- FOOTER --- */
        footer {
            border-top: 1px solid var(--border-dynamic);
            padding: 2rem 0;
            margin-top: auto;
            color: var(--text-muted);
            font-size: 0.85rem;
        }
        
        .footer-flex {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        @media (max-width: 600px) {
            .nav-flex, .footer-flex { flex-direction: column; gap: 1rem; align-items: flex-start; }
            nav a { margin-left: 0; margin-right: 1rem; }
            h1 { font-size: 1.75rem; }
        }
    </style>
</head>
<body>

    <header>
        <div class="container nav-flex">
            <a href="index.html" class="site-logo">SITE NAME</a>
            <nav>
                <a href="index.html">Index</a>
                <a href="#">About</a>
                <a href="#">Contact</a>
            </nav>
        </div>
    </header>

    <main class="container">
        
        <h1>Page Title</h1>
        <p class="lead">A brief subtitle or summary of the page content goes here to set the context for the user.</p>
        
        <hr>

        <section>
            <h2>Section Heading</h2>
            <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Use this template to build out new pages quickly while maintaining the existing design system.</p>
            
            <div class="card">
                <h3>Card Component</h3>
                <p>This is a standard content card (white in light mode, dark slate in dark mode). It is good for highlighting specific information.</p>
                <a href="#" class="btn">Call to Action</a>
            </div>
        </section>

        <section>
            <h2>Another Section</h2>
            <p>Continue your content here. The layout is responsive and will adjust to mobile screens automatically.</p>
            <ul>
                <li>Bullet point one</li>
                <li>Bullet point two</li>
                <li>Bullet point three</li>
            </ul>
        </section>

    </main>

    <footer>
        <div class="container footer-flex">
            <span>&copy; 2023 Site Name. All rights reserved.</span>
            <div>
                <a href="#" style="color: var(--text-muted); margin-left: 1rem;">Privacy</a>
                <a href="#" style="color: var(--text-muted); margin-left: 1rem;">Terms</a>
            </div>
        </div>
    </footer>

</body>
</html>